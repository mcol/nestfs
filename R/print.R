## display the important contents of the fs object
print.fs <- function(x, ...) {
  print(summary(x))
}

## display the important contents of the nestfs object
print.nestfs <- function(x, ...) {
  print(summary(x))
}
