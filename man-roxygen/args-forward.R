#' @param x Dataframe of predictors: this should include all variables in
#'        the initial set and the variables that are allowed to enter the
#'        selected panel.
#' @param init.model Either a formula or a vector of names of the initial set
#'        of variables that define the model from which the forward selection
#'        should start.
